# Copy to infra/redis/.env and replace with a long random value.
# Percent-encode this value when embedding it in REDIS_URL.
REDIS_PASSWORD=replace-with-at-least-32-random-characters
REDIS_PORT=6379
REDIS_MAXMEMORY=768mb
